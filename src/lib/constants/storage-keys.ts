/**
 * LocalStorage Keys
 *
 * Centralized constants for localStorage keys used throughout the application.
 * Using constants prevents typos and makes it easier to track localStorage usage.
 */

/**
 * Storage key for content plan view mode preference (grid/table)
 */
export const CONTENT_PLAN_VIEW_MODE_KEY = 'content-plan-view-mode';
